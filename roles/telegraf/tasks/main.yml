---
- yum:
    name: "{{ telegraf_download_path }}"
  sudo: true

- template:
    src: telegraf.conf.j2
    dest: "/etc/opt/telegraf/telegraf.conf"
    owner: telegraf
    group: telegraf
  sudo: true

- service:
    name: telegraf
    state: started
  sudo: true
